# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/medicare_db?useSSL=false
spring.datasource.username=root
spring.datasource.password=sparky1225
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false 
spring.jpa.properties.hibernate.format_sql=false  # Disable SQL formatting
spring.jpa.properties.hibernate.use_sql_comments=false  # Disable SQL comments
spring.jpa.open-in-view=false

# Server Configuration
server.port=8081

# --- Logging Configuration ---

# Set the root logger level to WARN to reduce log output
logging.level.root=WARN

# Optional: Set specific logging levels if needed
logging.level.org.springframework=WARN
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN

# Disable detailed Hibernate SQL logs
logging.level.org.hibernate.SQL=ERROR
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=ERROR
logging.level.org.hibernate.type=ERROR



















